import type {Entity,User} from '../../../types';
import {UserStatusBadge} from './UserStatusBadge';
export function UserTable({users,clients,onView,onEdit,onToggle}:{users:User[];clients:Entity[];onView:(user:User)=>void;onEdit:(user:User)=>void;onToggle:(user:User)=>void}){const names=(user:User)=>user.role==='Admin / Partner'?'System-wide':user.entity_ids.map(id=>clients.find(client=>client.id===id)?.name).filter(Boolean).join(', ')||'No client';return <div className="table-wrap users-table"><table><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Client</th><th>Status</th><th>Last login</th><th>Actions</th></tr></thead><tbody>{users.map(user=><tr key={user.id}><td><strong>{user.full_name}</strong>{user.mobile&&<small>{user.mobile}</small>}</td><td>{user.email}</td><td>{user.role}</td><td>{names(user)}</td><td><UserStatusBadge active={user.is_active}/></td><td>{user.last_login_at?new Date(user.last_login_at).toLocaleString():'Never'}</td><td><div className="row-actions"><button onClick={()=>onView(user)}>View</button><button onClick={()=>onEdit(user)}>Edit</button><button className={user.is_active?'danger':''} onClick={()=>onToggle(user)}>{user.is_active?'Deactivate':'Activate'}</button></div></td></tr>)}</tbody></table></div>}
