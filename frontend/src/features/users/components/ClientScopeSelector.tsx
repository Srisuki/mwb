import type {Entity} from '../../../types';
export function ClientScopeSelector({clients,value,required,onChange}:{clients:Entity[];value:string[];required:boolean;onChange:(value:string[])=>void}){return <fieldset><legend>Client / organization access{required?' *':''}</legend>{clients.length===0?<span className="muted">No active clients are available.</span>:clients.map(client=><label className="checkbox" key={client.id}><input type="checkbox" checked={value.includes(client.id)} onChange={()=>onChange(value.includes(client.id)?value.filter(id=>id!==client.id):[...value,client.id])}/>{client.name}</label>)}</fieldset>}
