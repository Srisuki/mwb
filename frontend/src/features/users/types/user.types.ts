export type UserFilters={search:string;role:string;client:string;status:string;page:number;page_size:number};
export type UserDraft={full_name:string;email:string;mobile:string;role_name:string;entity_ids:string[];is_active:boolean;temporary_password:string};
